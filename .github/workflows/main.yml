name: Cloudflare Gateway Sync

on:
  schedule:
    - cron: '0 * * * *' # Corrected: Runs at 04:00 UTC daily
  workflow_dispatch:
    inputs:
      force_mode:
        description: 'Force Update'
        type: boolean
        default: false
      delete_mode:
        description: 'Cleanup Mode'
        type: boolean
        default: false

jobs:
  run-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: pip install requests

      - name: Run Sync
        env:
          API_TOKEN: ${{ secrets.API_TOKEN }}
          ACCOUNT_ID: ${{ secrets.ACCOUNT_ID }}
        run: |
          ARGS=""
          [ "${{ github.event.inputs.force_mode }}" == "true" ] && ARGS="--force"
          [ "${{ github.event.inputs.delete_mode }}" == "true" ] && ARGS="--delete"
          python3 block_ads_sync.py $ARGS

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add *.txt
          if ! git diff --cached --quiet; then
            git commit -m "Update blocklists [skip ci]"
            git push
          fi
